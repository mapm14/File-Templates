package ${packageName}.presenter

import com.manuelperera.presentation.common.presenter.BasePresenter
import ${packageName}.view.${className}View


class ${className}PresenterImpl : BasePresenter<${className}View>(), ${className}Presenter {
    
    override fun initialize(view: ${className}View?) {
        super.initialize(view)
    }

}